#include "main.h"

/**
 * * int_abs(int i): Computes the absolute value of an integer.
 * * @i: Input number is an integer.
 * * Return: Absolute value.
 **/

int _abs(int i);
{
	if (i >= 0)
	{
		return (i);
	}
		else
	{
		return (i * -1);
	}
}
